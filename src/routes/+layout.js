/*
  Static rendering settings.
  GitHub Pages serves static files, so the full site is prerendered at build time.
*/
export const prerender = true;

/*
  Trailing slashes avoid redirect issues on static hosts such as GitHub Pages.
*/
export const trailingSlash = 'always';
